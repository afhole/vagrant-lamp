set timeout -1
spawn "/bin/bash"
match_max 100000
expect -exact "# "
send -- "pecl update-channels\r"
expect -exact "# "
send -- "pecl install oci8\r"
expect " compiling with Oracle Instant Client"
send -- "instantclient,<%= "#{node[:oracle_instantclient][:install_dir]}/#{node[:oracle_instantclient][:client_dir_name]}" %>\r"
expect "install ok:" 
send "exit\r"

